---
import Layout from "./Layout.astro";

interface Props {
    title: string;
    lastUpdated: Date;
    createdAt: Date;
    author: string;
}

const { title, createdAt, lastUpdated, author } = Astro.props;
---

<Layout title={`${title} | Nahal's Notes`}>
    <main class="h-screen w-fit m-auto p-6">
        <div class="fixed left-4 bottom-4 bg-slate-300/30 dark:bg-slate-700/30 rounded-full p-2 flex items-center justify-center backdrop-blur-sm">
            <svg
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke-width="1.5"
                stroke="currentColor"
                class="w-6 h-6"
            >
                <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M2.25 12l8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25"
                ></path>
            </svg>
        </div>
        <header class="mb-8">
            <h1
                class="text-5xl mb-2 dark:text-slate-300 font-bold tracking-wide"
            >
                {title}
            </h1>
            <p class="text-slate-500 text-sm">
                Created by {author} on {createdAt.toLocaleDateString()}, last
                updated on {lastUpdated.toLocaleDateString()}
            </p>
        </header>
        <article
            class="prose lg:prose-lg prose-slate dark:prose-invert prose-headings:tracking-wide"
        >
            <slot />
        </article>
    </main>
</Layout>
